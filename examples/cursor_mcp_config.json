{
  "mcpServers": {
    "netops-mcp": {
      "command": "python",
      "args": ["-m", "netops_mcp.server_http"],
      "env": {
                  "NETOPS_MCP_CONFIG": "/path/to/config.json"
      }
    }
  }
}
